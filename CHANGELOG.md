# Changelog

[Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) 形式に概ね準拠し、
[セマンティックバージョニング](https://semver.org/lang/ja/) に従います。

バージョンはコミット履歴からさかのぼって付けたもので、タグは打っていません。

## [Unreleased]

### Added
- 情報を表すバッジ `dm-badge--info`（青系）。テーマに `info` の色を足し、
  本文に `<span class="dm-badge dm-badge--info">…</span>` と書けば使える

### Fixed
- 項目の間を空けた箇条書き（中身が段落で包まれる書き方）で、1 行目が欄に
  分かれずカードにならなかった

## [0.4.0] - 2026-09-16

週次の 3 列で出す種類 `weekly3` を追加しました。

### Added
- 週次の 3 列で出す種類 `weekly3`。見出し 2 を 4 つ書く前提で、1 つ目を横幅いっぱいの
  1 列（トピックス）、2 〜 4 つ目を 3 列として組み立てる。見出しの文字列ではなく
  位置だけで決める。4 つでなければ警告する（処理は続く。`--strict` で失敗扱い）
- 列の中の小見出し（`###`）ごとにまとめ直し、その中に列を並べる（`group_columns`）。
  ある分類が一部の期間にしか無い場合も、列の位置がずれないよう枠だけ残す
- サブ項目（`####`）ごとにもう一段まとめ直し、サブ項目ごとに列を並べる
- 小見出しを持たない 1 つ目の節を、横幅いっぱいの 1 列として残す（トピックス）
- トピックスを小見出し（`###`）ごとの枠に分ける（`topic_cards`）。枠は縦に積み、
  枠の中は段落・箇条書き・表を何行でも書ける
- `ラベル:数値 / ラベル:数値` だけの段落を件数の並びにする（`count_summary`）。
  サブ項目ごとにも、分類全体にも書ける
- `見出し｜属性｜説明` のリスト項目を 1 件のカードにする（`entry_card`）。属性のうち
  状態を表す語はバッジになる
- 1 件ごとにコメントを添えられるようにした。項目の下に字下げして書いた内容
  （入れ子の箇条書き・段落）が、カードの下段に入る
- front matter に `開始日` を書くと `期間` を 1 週間分として組み立てる
  （`period_range`）。`期間` を直接書く書き方もそのまま使える。両方ある場合は
  `期間` を優先して警告する。日数は `PERIOD_DAYS`、キー名は `keywords` で変えられる
- 列の見出しに `（m/d〜m/d）` を自動で補う（`column_periods`）。1 つ目の列を
  front matter の期間とみなし、前後の列を 1 週間ずつずらす
- ルールが警告を出せるようにした（`rules.warn`）。変換結果の警告に混ざり、
  `--strict` の対象にもなる
- フッタの種類名を `{% block footer %}` で差し替えられるようにした。`weekly3` は
  見比べる紙面なのでフッタを出さない
- 状態バッジの検出語に「再オープン」「期限超過」「期限切れ」を追加
- サンプル `examples/課題サマリー.md`。トピックスの枠に置いた表、列の自由記述、
  区切りの数ごとのカードを含む

### Changed
- 種類の名前を `columns` から `weekly3` に変更。見た目ではなく、週次の 3 列という
  用途で呼ぶ
- ルールの置き場を種類名に揃えた（`rules/columns.py` → `rules/weekly3.py`）。
  テンプレート・CSS クラス・ルール名は構造を指す名前なのでそのまま
- まとまり・列・サブ項目の見出しを帯（背景色）にし、段ごとに濃さを変えた。
  どこからどの見出しの内容かが面で分かる
- 列の名前をサブ項目の行ごとに出すようにした。まとまりの中で 1 回だけだと、
  下の行でどの列がどれか分からなくなるため
- トピックの枠を縦に積むようにした。横に並べると、行数の違う枠が凸凹に見えるため
- 1 列の節から外枠を外した。中の枠と二重に見えるため
- `config.example.yaml` の検出語一覧を作り直し、グループの増減に追随しているかを
  テストで見るようにした

### Fixed
- 区切りのある項目と普通の項目が混ざった箇条書きで、普通の項目まで行頭記号と
  字下げを失っていた

## [0.3.0] - 2026-09-16

図・印刷・本文の安全性に対応しました。

### Added
- Mermaid の図に対応。```mermaid のコードブロックを描画対象の要素に変換する。
  mermaid.js は CDN 参照（`mermaid: cdn`）と同梱の埋め込み（`mermaid: true`）から
  選べる（既定は無効。CDN 参照にすると、その種類の HTML は自己完結ではなくなる）
- `figure_caption` が画像だけでなく Mermaid の図も採番し、直前の「図: 説明」を
  キャプションにする。同梱の `spec` は `mermaid: cdn` を使い、サンプル
  `examples/設計書.md` に図を 2 つ入れてある
- 本文の生 HTML を許可リストで絞る（既定 `sanitize: strict`）。`script` / `onerror` /
  `javascript:` などを落とし、`<br>` や表の桁揃えは残す。トップレベルの `sanitize` が
  全体の既定、`profiles.<type>.sanitize` が種類ごとの上書き
- 印刷時にページ番号を下端に入れる（表紙には出さない）

### Fixed
- 出力フォルダを排他的に作成する。同じ秒に複数のプロセスが走っても、フォルダを
  共有して出力が混ざることがない（連番が付く）

## [0.2.0] - 2026-09-16

変換まわりを整備し、取りこぼしを警告するようにしました。

### Added
- 依存が足りない Python で起動された場合、トレースバックではなく実行中の
  インタプリタと導入コマンドを案内して終了する（`deps.py`）
- 文書間リンク（`[設計書](設計書.md)`）を `.html` に向け直す。フラグメントとクエリは
  温存し、外部 URL とページ内アンカーは書き換えない
- `--strict` で警告を失敗扱いにする（終了コード 1）。バッチや定期実行で、type の
  タイプミスや画像の欠落を取りこぼさないため
- ルールの検出語（「出席者」「ToDo」「時刻」「状態」など）を `config.yaml` の
  `keywords:` で差し替え可能。表記ゆれへの対応でコードを直さずに済む
- 索引を front matter の日付の新しい順に並べ、日付を持たないものは最後にまとめる
- `--reproducible` で生成時刻を埋め込まない（索引を差分管理したいとき）
- front matter のキーの打ち間違いを警告する（`titel` → `title`）。自由に書いた項目は
  指摘せず、意味を持つキーによく似たものだけを対象にする
- 入力が複数のときに `-o` へ `.html` を渡すと警告する（その名前のディレクトリが
  できることが分かりにくいため）

### Fixed
- 別ディレクトリの同名ファイルは出力先をずらして両方残す（`doc.html` /
  `doc-b.html`）。以前は黙って上書きされ、片方の結果が消えていた
- front matter に `title` が無い場合、見出しに差し込んだ章番号や手順番号が
  タイトルに混ざっていた（`spec` で「1概要」など）

## [0.1.0] - 2026-09-16

最初の実装。

### Added
- Markdown を種類ごとの表現で自己完結 HTML に変換する docmold 本体
- front matter の `type` によるプロファイル切り替え（明示指定のみ。パス名や本文からの
  自動判定は行わない。未知の `type` は警告のうえ `default` で処理）
- 3 層分離（テンプレート / ルール / テーマ）と YAML 駆動のプロファイル定義。同梱
  `profiles.yaml` にユーザ `config.yaml` を再帰マージする（差分だけ書けばよい）
- 同梱プロファイル `default` / `minutes` / `procedure` / `incident` / `spec` / `weekly`
- ルール層のレジストリ（`@rule` デコレータ）。種類の追加は「YAML 数行 + 関数 1 つ」
  - 議事録: `attendee_table` / `todo_checklist` / `decision_highlight`
  - 手順書: `step_numbering` / `command_block_copy` / `rollback_callout`
  - 障害報告: `severity_badge` / `impact_summary` / `timeline_table`
  - 設計書: `figure_caption` / `table_caption` / `cross_reference`
  - 週次報告: `status_badge` / `progress_bar`
- 完全自己完結 HTML の出力（CSS / JS / 画像 base64 を埋め込み、外部参照ゼロ）
- 印刷用 `@media print`（手順書は見出しごとに改ページ、設計書は表紙を 1 ページ目に、
  障害報告は DRAFT 透かし）
- ディレクトリ再帰の一括変換と索引 HTML の生成（`--index`）
- `--list-types` / `--list-rules`、`--type` による上書き、`--dry-run`
- 対話メニュー（`menu.py` / `menu.bat`）
- 既定の出力先は docmold 同梱の `out/<YYYYMMDD-HHMMSS>/`（起動位置に依存せず、
  実行ごとにフォルダを分離）。`-o` を指定した場合はそのパスをそのまま使う
- サンプル HTML（`examples/html/`）をリポジトリで管理。`tools/build_examples.py` で
  再生成し、古くなると `tests/test_examples.py` が落ちて再生成を促す
